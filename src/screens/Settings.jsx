import { useRef, useState } from 'react'
import Icon from '../components/Icon'
import { PageHeader } from '../components/UI'
import { initialData } from '../lib'
export default function Settings({data,setData,reset}){const input=useRef(),[confirm,setConfirm]=useState(false),[message,setMessage]=useState('');const exportData=()=>{const blob=new Blob([JSON.stringify({...data,exportedAt:new Date().toISOString()},null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`bjj-notebook-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(a.href)};const importData=e=>{const file=e.target.files[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{try{const parsed=JSON.parse(reader.result);if(!Array.isArray(parsed.sessions)||!Array.isArray(parsed.techniques)||!Array.isArray(parsed.sparring))throw Error();setData({...initialData,...parsed,gamePlan:{...initialData.gamePlan,...parsed.gamePlan}});setMessage('Notebook imported successfully.')}catch{setMessage('That file is not a valid BJJ Notebook export.')}};reader.readAsText(file)};const doReset=()=>{reset();setConfirm(false);setMessage('All notebook data was removed from this device.')};return <><PageHeader eyebrow="Local notebook" title="Settings & data" description="Your notes live only in this browser. Export a backup before clearing browser data or moving devices."/>{message&&<div className="notice">{message}</div>}<div className="settings-list"><article className="setting-row"><div className="setting-icon"><Icon name="download"/></div><div><h3>Export notebook</h3><p>Download every session, technique, sparring note, and game plan section as JSON.</p></div><button className="secondary" onClick={exportData}>Export JSON</button></article><article className="setting-row"><div className="setting-icon"><Icon name="upload"/></div><div><h3>Import notebook</h3><p>Restore a BJJ Notebook JSON backup. Current data will be replaced.</p></div><input ref={input} hidden type="file" accept="application/json" onChange={importData}/><button className="secondary" onClick={()=>input.current.click()}>Choose file</button></article><article className="setting-row danger-zone"><div className="setting-icon"><Icon name="trash"/></div><div><h3>Reset app</h3><p>Permanently remove all notebook data stored in this browser.</p></div>{confirm?<div className="confirm"><button className="ghost" onClick={()=>setConfirm(false)}>Cancel</button><button className="danger-button" onClick={doReset}>Confirm reset</button></div>:<button className="danger-outline" onClick={()=>setConfirm(true)}>Reset app</button>}</article></div><div className="privacy-note"><Icon name="book"/><div><h3>Private by design</h3><p>No account, analytics service, or remote database. Your training notes never leave this device unless you export them.</p></div></div></>}
